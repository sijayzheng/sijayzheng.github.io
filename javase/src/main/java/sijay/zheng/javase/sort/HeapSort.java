package sijay.zheng.javase.sort;

import java.util.Arrays;

/**
 * @author Sijay
 * 堆排序
 */
public class HeapSort {
    public int[] sort(int[] arr) {
        int[] result = Arrays.copyOf(arr, arr.length);
        return arr;
    }
}
